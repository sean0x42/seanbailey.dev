/** @format */

import { style } from 'treat'

export const wrapper = style({
  // Positioning
  position: 'relative',

  // Box model
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'space-between',
  paddingTop: '1rem',
  paddingBottom: '1rem',
})
